<h1><%= @doctor.name %></h1>

<h2>Appointments:</h1>
  <% @doctor.appointments.each do |appointment| %>
    <%= link_to appointment.patient.name, patient_path(appointment.patient.name) %> said
    <%= comment.content %>
  <% end %>
