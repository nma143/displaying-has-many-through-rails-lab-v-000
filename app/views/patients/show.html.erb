<h1>@patient.name</h1>

<h2>Appointments:</h2>
<ul>
  <% @patient.appointments.each do |appointment| %>
    <li>
      Name: <%= link_to patient.name, patient_path(patient) %> Number of Appointments: <%= patient.appointments.count %>
  </li>
  <% end %>

</ul>
