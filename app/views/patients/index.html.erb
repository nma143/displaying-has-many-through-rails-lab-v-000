<h1>Patient List</h1>

<ul>
  <% @patients.each do |patient| %>
    <li>
      <%= link_to patient.name, patient_path(patient) %> has <%= pluralize(patient.appointments.count, 'appointment') %> %>
  </li>
  <% end %>

</ul>